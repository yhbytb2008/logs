# AGENTS.md - AI 行为约束

本项目是 `github.com/injoyai/logs`，一个基于 `log/slog` 封装的 Go 日志库。

---

## 核心原则

### 1. 禁止私自修改用户改动过的代码

用户修改过的代码代表用户的最终决定，AI **不得**在后续操作中将其改回。

- 如果 AI 生成了某段代码，用户觉得不合理并亲自修改了，AI 必须尊重用户的选择
- 后续改动遇到用户修改过的区域时，只能在此基础上扩展，不能推翻或"顺手改回去"
- 如果不确定某段代码是否被用户手动修改过，**先问用户**，不要假设

> **你的设计偏好不等于用户的需求。用户改掉的代码，说明用户不认可你的方案。**

### 2. 以项目整体视角理解需求

不要为了满足单一需求而引入与项目整体风格不协调的设计。

- 新增的函数/类型/接口必须与现有代码的命名风格、结构模式、抽象层级保持一致
- 不要为了"更通用"而引入项目中从未使用过的设计模式或抽象
- 不要因为一个需求就添加一个只有该需求用得上的泛化层/工具函数/中间对象
- 新增代码应该让不熟悉该需求的人读起来也觉得"这本来就该在这里"

**判断标准**：如果一段代码删掉需求描述后看起来像多余的设计，那它就不该存在。

---

## 项目约定

### 代码风格

- **包名**：`logs`，所有文件 `package logs`
- **注释**：中文注释，简洁说明意图
- **分隔线**：用 `//============ 标题 ============` 组织代码段
- **Setter 链式调用**：所有 Setter 返回 `*Entity` 以支持链式调用
- **线程安全**：`Entity` 使用 `sync.RWMutex` 保护字段，Getter 用 RLock，Setter 用 Lock
- **命名**：
  - 导出函数用大写驼峰（如 `NewEntity`、`SetLevel`）
  - 内部方法用小写驼峰（如 `setCaller`、`rebuildHandlers`）
  - 变量名简洁但不缩写过度（`levelVar` 而非 `lv`）
- **文件命名**：按功能拆分，如 `write_file.go`、`write_http.go`

### 架构约定

- `Entity` 是核心类型，包装 `slog.Logger` + `multiHandler`
- `multiHandler` 是 `slog.Handler` 的多路复用实现，内部持有多个子 handler
- Writer 通过实现 `io.Writer` 和可选的 `ColorWriter` 接口接入
- 格式化通过 `Formatter` 接口控制，内置 `DefaultFormatter`、`TimeFormatter`、`JsonFormatter`

### 不要做的事情

- 不要引入 `init.go` 以外的 `init()` 函数
- 不要添加项目中未使用过的第三方依赖
- 不要添加只有当前需求用得上的泛化抽象层
- 不要重构用户没有要求改动的代码
